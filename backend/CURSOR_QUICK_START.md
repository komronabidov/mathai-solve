# 🚀 Быстрый запуск в Cursor

## Запуск сервера одной кнопкой

### Способ 1: Через кнопку Run (▶️)
1. Откройте файл `backend/server.py` в Cursor
2. Нажмите кнопку **Run** (▶️) в правом верхнем углу
3. Сервер запустится автоматически на `http://127.0.0.1:5502`

### Способ 2: Через Debug панель (F5)
1. Нажмите `F5` или откройте Debug панель
2. Выберите конфигурацию "MathAI Backend (порт 5502)"
3. Нажмите зеленую кнопку запуска

### Способ 3: Через встроенный терминал
```bash
source .ocr_venv/bin/activate
cd backend
python server.py
```

## Что делает сервер

✅ Автоматически загружает настройки из `.env`  
✅ Запускается на порту 5502  
✅ CORS настроен для `http://localhost:3000`  
✅ Все зависимости уже установлены  
✅ OCR для распознавания математики из изображений  
✅ Построение графиков с помощью matplotlib  

## Проверка работы

После запуска откройте в браузере:
```
http://127.0.0.1:5502/api/health
```

Должен вернуться JSON:
```json
{"status": "ok"}
```

## Горячие клавиши в Cursor

- **Запуск**: `F5` или кнопка Run ▶️
- **Остановка**: `Shift+F5` или `Ctrl+C` в терминале

## Устранение проблем

Если сервер не запускается, проверьте:
1. Все зависимости установлены: `pip install -r requirements.txt`
2. Порт 5502 свободен
3. Python 3.x установлен: `python3 --version`
